<UserControl x:Class="TDM.Views.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:TDM.ViewModels"
             xmlns:conv="clr-namespace:TDM.Converters"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="700"
             Background="Transparent">
    <UserControl.Resources>
        <conv:BytesToStringConverter x:Key="BytesToStr"/>
        <conv:StatusToBrushConverter x:Key="StatusToBrush"/>
        <conv:CountToVisibilityConverter x:Key="CountToVis"/>
    </UserControl.Resources>
    <UserControl.DataContext>
        <vm:HistoryViewModel/>
    </UserControl.DataContext>

    <Grid Margin="24,20,24,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 标题 -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="下载历史" Style="{StaticResource H1}"/>
            <TextBlock Text="查看所有已完成的下载记录" Style="{StaticResource BodySecondary}" Margin="0,2,0,0"/>
        </StackPanel>

        <!-- 工具栏 -->
        <Border Grid.Row="1" Style="{StaticResource CardBorder}" Padding="14,10" Margin="0,0,0,14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="FilterBox" Grid.Column="0" Text="{Binding Filter, UpdateSourceTrigger=PropertyChanged, Delay=300}"
                         Style="{StaticResource ModernTextBox}" MaxWidth="320" HorizontalAlignment="Left"
                         ToolTip="搜索文件名或 URL"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Style="{StaticResource GhostButton}" Content="打开目录" Command="{Binding OpenFolderCommand}" Padding="12,5" Margin="0,0,4,0"/>
                    <Button Style="{StaticResource GhostButton}" Content="复制链接" Command="{Binding CopyUrlCommand}" Padding="12,5" Margin="0,0,4,0"/>
                    <Button Style="{StaticResource SecondaryButton}" Content="重新下载" Command="{Binding RetryCommand}" Padding="12,5" Margin="0,0,4,0"/>
                    <Button Style="{StaticResource GhostButton}" Content="删除" Command="{Binding RemoveCommand}" Padding="12,5" Margin="0,0,4,0"/>
                    <Button Style="{StaticResource DangerButton}" Content="清空" Command="{Binding ClearCommand}" Padding="12,5"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 表格 -->
        <Border Grid.Row="2" Style="{StaticResource CardBorder}" Padding="0">
            <Grid>
                <DataGrid ItemsSource="{Binding EntriesView}" SelectedItem="{Binding SelectedEntry}"
                          Style="{StaticResource ModernDataGrid}" BorderThickness="0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="文件名" Binding="{Binding FileName}" Width="*">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="大小" Binding="{Binding Size, Converter={StaticResource BytesToStr}}" Width="100">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="FontFamily" Value="{StaticResource MonoFontFamily}"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTemplateColumn Header="状态" Width="90">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border CornerRadius="10" Padding="10,3" HorizontalAlignment="Left" VerticalAlignment="Center"
                                            Background="{Binding Status, Converter={StaticResource StatusToBrush}}">
                                        <TextBlock Text="{Binding Status}" Foreground="White" FontSize="11" FontWeight="SemiBold"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="开始时间" Binding="{Binding StartTimeText}" Width="140">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="FontSize" Value="12"/>
                                    <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="完成时间" Binding="{Binding EndTimeText}" Width="140">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="FontSize" Value="12"/>
                                    <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- 空状态 -->
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center"
                            Visibility="{Binding Entries.Count, Converter={StaticResource CountToVis}, ConverterParameter=invert}">
                    <Border Width="80" Height="80" CornerRadius="40" Background="#1A4F8BFF" HorizontalAlignment="Center">
                        <Path Width="36" Height="36" Stretch="Uniform" Fill="{StaticResource PrimaryBrush}"
                              Data="M12,3 v10 M7,9 l5,5 5,-5 M5,21 h14" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <TextBlock Text="暂无下载历史" Style="{StaticResource H3}" HorizontalAlignment="Center" Margin="0,16,0,4"/>
                    <TextBlock Text="完成的下载会出现在这里" Style="{StaticResource BodySecondary}" HorizontalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>